#include<bits/stdc++.h>
using namespace std;
class Solution {
public:
    int numDistinct(string s, string t) {
        int lens = s.length();
        int lent = t.length();
        int dp[lens][lent];
        memset(dp, 0, sizeof(dp));
        for(int i = 0; i < lens; i++) {
            for(int j = 0; j < lent; j++) {
                
            }
        }
    }
};